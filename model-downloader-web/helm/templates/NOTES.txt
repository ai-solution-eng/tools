HF Model Downloader installed in namespace "{{ .Release.Namespace }}".

HPE proxy env:  {{ include "model-downloader.pcaiEnabled" . }}
Kyverno policy: {{ include "model-downloader.kyvernoEnabled" . }}
Ezua ingress:   {{ include "model-downloader.ezuaEnabled" . }}
Storage:        {{ .Values.storage.backend }} (default: {{ .Values.storage.default }})
Max downloads:  {{ .Values.maxConcurrency }}

Service:  {{ include "model-downloader.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}

Port-forward to use the UI:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "model-downloader.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  open http://localhost:{{ .Values.service.port }}

{{- if eq (include "model-downloader.pcaiEnabled" .) "true" }}
PCAI proxy env vars on downloader Jobs: ENABLED
{{- else }}
PCAI proxy env vars on downloader Jobs: DISABLED
{{- end }}

The downloader Job manifest is rendered from a ConfigMap
({{ include "model-downloader.fullname" . }}-job-template). To change the Job
definition, edit .Values.downloader.* / .Values.pcai.* / .Values.s3.* and
re-run helm upgrade.

NOTE: queue state is in-process memory. If the app pod restarts, queued/running
records disappear from the UI but the underlying k8s Jobs keep running and clean
themselves up via ttlSecondsAfterFinished ({{ .Values.downloader.ttlSecondsAfterFinished }}s).